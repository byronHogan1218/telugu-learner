<div>
  <div>
    <button mat-button (click)="goToQuestion()">Go to Question</button>
  </div>
  <div>
    <button mat-button (click)="start()">Start at random location</button>
  </div>
  <div>
    <button mat-button (click)="startRandom()">Start at random location</button>
  </div>
</div>
